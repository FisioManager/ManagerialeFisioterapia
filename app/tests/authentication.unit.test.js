jest.mock('bcryptjs')
jest.mock('../models/user.model.js')

describe('register', () => { 
    test('Dummy test, change it', () => {
        expect(true).toBe(true)
    })
})

describe('login', () => { 
    test('Dummy test, change it', () => {
        expect(true).toBe(true)
    })
})
